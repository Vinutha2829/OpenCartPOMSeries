git init -->used to intialize git
git remote add origin <repo url>  -->to make connection between git and local copy
git add . -->to add full code / git add <file name> -->to add particular file -->available at stage area
git commit -m "reason" --> code will be available at local repo
git push origin master -->code will push to remote master
-------

git branch --> to check branch
git branch cartpage --> to create branch
git checkout cartpage --> switch to cartpage

git pull origin master 
git clone <repo url>

--------------
git log  //to check commits history
git log -3 //to check last 3 commits hostory
git log --oneline //provide oneline commits
git log --author="name" //provides only particular author history
git log --before="date"//provides commits history particular before history
git log --grep="cart" //provides commits based on cart
git log --state
git log --reverse// first to last


git show 2serr /particular id commits
git show HEAD /HEAD means latest commit
git show HEAD~2 
git shortlog /provides each and every members commit history
git blame "part of code" / provides each and every line in a code



git branch
git branch naveen
git checkout naveen
git branch --delete naveen //delete branch from local
git branch -r /switch to remote
git bracnh -vv /provides local and remote branch details
git push -d origin naveen //delete branch from remote side



git add <filename>
git commit -m "reason"
git tag v1.0
git push origin v1.0

